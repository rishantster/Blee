# Blee Android requires no Solana provider credentials.
#
# Production Solana RPC gateway:
# https://rpc.paywithblee.xyz/api/solana
#
# HELIUS_API_KEY is server-side only in the paywithblee Vercel project.
# Never add Helius API keys, Helius RPC URLs, or provider secrets to APK-bound env files.
