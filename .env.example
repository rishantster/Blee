# Helius Secure RPC URL from Dashboard → RPC → Secure URL.
# This must be the masked frontend/mobile endpoint, never an ?api-key= URL.
NEXT_PUBLIC_BLEE_HELIUS_SECURE_RPC=https://YOUR-SECURE-ENDPOINT-fast-mainnet.helius-rpc.com
